#include "monty.h"

/**
 * m_nop - Does nothing.
 * @head: Pointer to the head of the stack.
 * @line: Line number of the Monty bytecodes file.
 */
void m_nop(stack_t **head, unsigned int line)
{
	(void)line;
	(void)head;
}
