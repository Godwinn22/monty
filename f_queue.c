#include "monty.h"

/**
 * f_queue - sets the mode to queue
 * @head: pointer to the head of the stack
 * @line: line number
 */
void f_queue(stack_t **head, unsigned int line)
{
	(void)head;
	(void)line;
	transpInfo.trficIdx = 1;
}
