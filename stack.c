#include "monty.h"

/**
 * m_stack - sets the format of the data to a stack (LIFO)
 * @head: double pointer to the stack_t node
 * @line: line number of the opcode in the file
 */
void m_stack(stack_t **head, unsigned int line)
{
	(void)head;
	(void)line;
	transpInfo.trficIdx = 0;
}
